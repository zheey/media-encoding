const {
    handleError
  } = require("../helpers/utility");
  
  const getAPIInfoDAO = async (data) => {
    try {
      
    } catch (error) {
      return handleError(error);
    }
  };

  module.exports = {
    getAPIInfoDAO,
  };
  