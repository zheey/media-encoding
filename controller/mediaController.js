const {
  sendErrorResponse,
  sendSuccessResponse
} = require("../helpers/utility");
const {
    getAPIInfoDAO,
  } = require("../dao/mediaDAO");

const getAPIInfo = async (req, res) => {
  
};

module.exports = {
  getAPIInfo
};
